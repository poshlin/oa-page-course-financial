# DESIGN-v2.md — 財商 landing page 第二版（`v2.html`）

`v2.html` 是與 `index.html` **並存**的實驗版本，用來測「把說明性文字收進分頁／圖示條列，能不能讓整頁更好讀又不掉留單」。
`index.html` 一個字都沒動。本檔只記 v2 與 v1 的差異；共通規範（色票、圓形舞台、疑問句卡、hero 四色波浪、實拍與影片規則）一律回去看 `DESIGN.md`。

建立日期：2026-08-23

---

## 0. 三條不可逾越的界線

這三條決定了 v2「藏字」藏到哪裡為止。任何後續改動都要先過這三關。

1. **摺疊可以，換成圖不行。**
   `<details>`、分頁面板隱藏的內容**仍在 DOM 裡**，Google 行動優先索引會給完整權重，AI 爬蟲讀原始碼也讀得到。
   但只要把一句話改成一個圖示，那句話就是真的消失了——SEO 掉關鍵字，AEO/GEO 掉可引用的句子。
   ⇒ **v2 沒有任何一段文字被圖示取代。** 每個圖示旁邊都有原本那句話。
   ⇒ 相對的，**絕對不可以改成「點開才用 JS 載入內容」**——那才是真的把內容藏起來。

2. **`#tldr` 的文字一個字都不能動。**
   它是 schema 裡 `speakable` 的第一個 `cssSelector`，也是 AI 摘錄本頁時最主要的來源。
   v2 只把那一整段拆成「一句結論 ＋ 四條事實」，**四條串起來與 v1 逐字相同**（已用腳本比對確認）。

3. **FAQ 的題目與答案一個字都不能動。**
   同一份文字同時存在於頁面與 `FAQPage` schema。v2 只重排順序、加組標，14 題的 `<summary>` 與 `.faq-a` 全部逐字沿用。
   要改文案，**頁面與 schema 必須同時改**。

`DESIGN.md` §107（2026-08-14，階段卡由兩層摺疊改一層）記錄的判斷仍然有效：
**45 堂課表是全頁可信度最高的證據，不可以被藏到需要點兩次才看得到的深度。**
v2 用分頁而不是再加一層摺疊，就是為了守住這一條——分頁的畫面上永遠有一整個階段的完整課表，不點也看得到。

---

## 1. 六項差異

| # | 區塊 | v1 | v2 |
|---|---|---|---|
| 1 | `#tldr` | 一段 230 字 | 一句結論 ＋ 四條圖示事實（**文字逐字相同**） |
| 2 | `#syllabus` 定義段 `.defbox` | 兩段共約 250 字 | 一段約 60 字 ＋ 三條圖示條列，共約 115 字 |
| 3 | `#syllabus` 三階段 | 三張卡直排，桌機全展開 | **桌機分頁（tab）**，一次一個階段；手機維持摺疊卡 |
| 4 | `#course` 六種關卡 | 一整段 140 字長句 | 六格圖卡；手機為橫向滑動列 |
| 5 | `#faq` | 14 題平鋪雙欄 | 依主題分**四組**，每組一個帶圖示的組標與題數 |
| 6 | 全頁 | 無任何追蹤 | 無 ID 的量測腳手架（`window.dataLayer`） |
| 7 | `#class` 三張卡、`#practice` 步驟 04–06 | 手機直排 | 手機改**橫向滑動列**（見 §4） |

另有兩處小改：`#report` 的 ★ 與 `#fit` 的 ✓ 換成具體圖示；`#class` 三張卡的 `<h3>` 加上圖示。

---

## 2. 三階段分頁（`#phaseTabs`）

### 行為

| 視窗 | 分頁列 | 內容 |
|---|---|---|
| ≥ 861px | 顯示 | 一次只顯示一個階段，另兩個 `hidden`（仍在 DOM） |
| ≤ 860px | `display: none` | 三張卡直排，`details.phase-more` 收合（沿用 v1 的既有 script） |
| 無 JS | `display: none` | 三張卡直排、**全部展開**（HTML 寫死 `open`） |

🔴 **安全降級的方向不可顛倒**：沒有 JS 時是「全部看得到、頁面比較長」，不是「全部藏起來」。這條與 `DESIGN.md` §119 同源。

### 實作要點

- `role` / `aria-selected` / `aria-controls` / `tabindex` **一律由 JS 在 ≥861px 時掛上、離開斷點時移除**。
  不可以寫死在 HTML——手機與無 JS 時那三張卡就是普通的堆疊卡片，掛著 `tablist` 語意等於對輔助科技說謊。
- 鍵盤：←／→ 循環、Home／End 跳頭尾，roving tabindex（只有選中的分頁 `tabindex=0`）。
- 分頁模式下三個 `details.phase-more` 一律 `open = true`。
- **不需要**再寫一條 CSS 去隱藏 `summary`：既有的 `details.phase-more > summary { display: none }` 已經把它藏在桌機，
  只有 ≤860px 的 media query 會放出來，而分頁模式一律在 ≥861px。多寫同義規則只會讓人誤以為是分頁在控制它。
- 分頁鈕選中態＝實心 `--primary` 配 `--text` 深墨字（**不配白字**，沿用 `DESIGN.md` 的 CTA 紀律）。

### 摺疊標題要帶數量

手機的 `<summary>` 從「看這個階段的學習重點與完整課表」改成「看這個階段的學習重點與 **15 堂**完整課表」。
家長真正需要的是「知道有這麼多堂」這個事實，讀不讀是其次——摺疊標題寫「了解更多」等於把這個事實一起藏掉。

---

## 3. 圖示系統

- 位置：`<body>` 開頭一組 `<symbol>`（18 個），用 `<svg class="ico"><use href="#i-xxx"></use></svg>` 引用。
- 一律**單色描邊**、吃 `currentColor`，顏色只用既有 token（多為 `--coral-ink`）。**沒有新增任何色票。**
- 🔴 **彩色 emoji 全禁**（沿用品牌紅線），所以只能用 inline SVG。
- 🔴 **圖示不承擔語意**：全部 `aria-hidden="true"`，旁邊必須有可被爬蟲讀到的文字。
  圖示是視覺錨點，不是內容——抽象圖示的辨識率遠低於一句話。

---

## 4. 手機的卡片列一律走 `.swipe`

**這一節是「手機不能變長」這條要求的主要來源。**
圖示條列與 FAQ 分組在手機是淨增高（合計約 +200px），必須從別的地方賺回來。
做法不是刪內容，而是把**原本直排的卡片列**接上本頁既有的 `.swipe` 橫向滑動模式。

v2 新增三條滑動列（原本有三條，共六條）：

| 位置 | 直排高度 | 改滑動後 |
|---|---|---|
| `#course` 六種關卡 `.typegrid` | 六張卡 ≈ 660px | 一張卡 ≈ 160px |
| `#class` 三張卡 `.grid-3` | 約 540px | 約 190px（`#class` 1,343 → 956） |
| `#practice` 步驟 04–06 `.flow-2` | 約 450px | 約 190px（`#practice` 3,819 → 3,505） |

`#practice` 這條還順帶修掉一個不一致：v1 是上面的步驟 01–03 可以滑、下面的 04–06 直排。

### 🔴 步驟徽章會被滑動列裁掉

`.step-num`（01–06 那顆橘色徽章）是 `top: -18px` 的絕對定位，
而滑動列的 `overflow-x: auto` 會讓 **`overflow-y` 一併變成 `auto`** ——只有 4px 上內距時，徽章上緣會被切掉。

**v1 就有這個缺陷**：實測步驟 01–03 那條列 `overflowY=auto`、徽章被切 **10px**（v1 的 04–06 因為不是滑動列所以沒事）。
v2 把兩條列一起修好：

```css
@media (max-width: 860px) {
  .flow.swipe { padding-top: 22px; }   /* 容得下 top:-18px 的徽章 */
  .flow-2.swipe { margin-top: 6px; }   /* 把多出來的高度收回來 */
}
```

實測修正後兩條列的徽章裁切量都是 **0px**。⚠️ 這個缺陷 `index.html` 仍然存在，要不要一併修是另一個決定。

### `.typegrid` 的特異性陷阱

`.typegrid` 在桌機是三欄圖格；**手機一定要接上 `.swipe`**，否則六張卡直排會讓手機頁長多出約 500px。

⚠️ CSS 陷阱：`.typegrid` 與 `.swipe` 特異性相同，而 v2 的樣式區塊排在既有 `.swipe` 規則**之後**，
所以必須在 v2 自己的 media query 裡把 `display: flex` 明確搶回來，否則橫向滑動不會成形：

```css
@media (max-width: 860px) {
  .typegrid.swipe { display: flex; grid-template-columns: none; }
  .typegrid.swipe > li { flex: 0 0 62%; }
}
```

`role="group"` / `tabindex` / `aria-label` 由既有的 `.swipe[data-swipe-label]` script 自動掛上，不必另外處理。

---

## 5. 量測腳手架

目前**沒有接任何 GA4／GTM／pixel**，所以這段是完全無副作用的空轉——只把事件 push 進 `window.dataLayer`。
日後在頁面掛上容器，這些事件直接可用，不必再動版面程式。

| 事件 | 何時送 | 附帶欄位 |
|---|---|---|
| `oa_cta_click` | 點任何 `[data-oa-cta]` | `cta_position`（hero／syllabus／middle／bottom／sticky／nav） |
| `oa_scroll_depth` | 捲過 25／50／75／100% | `depth` |
| `oa_faq_open` | 展開 FAQ | `item`（題目前 60 字） |
| `oa_detail_open` | 展開其他 `<details>` | `item` |
| `oa_phase_tab` | 切換階段分頁 | `phase`（1／2／3） |

所有事件都帶 `page_variant: 'v2'`，方便與 v1 對照。

### 兩個踩過的坑

1. 🔴 **`h <= 0` 絕對不可以當成「已捲到底」。**
   本腳本放在 `<body>` 開頭，執行當下頁面還沒解析完，可捲動高度是 0。
   舊寫法把這種情況的 `pct` 算成 100，於是**每一次載入都立刻回報 25/50/75/100 全達成**，數據等於全毀。
   現在是 `if (h <= 0) return;`。已用 Firefox headless 實測確認修好。

2. **`toggle` 事件不會冒泡**，必須用 `document.addEventListener('toggle', fn, true)` 走捕獲階段。
   寫成一般的冒泡監聽會完全收不到。

---

## 6. 實測數字（Firefox headless，2026-08-24）

| | v1 | v2 | 差 |
|---|---|---|---|
| 桌機（1440px）全頁高度 | 16,577px | **15,410px** | **−1,167px（−7.0%）** |
| 手機（390px）全頁高度 | 17,880px | **17,382px** | **−498px（−2.8%）** |
| 桌機可見的課表列數 | 45 | **15** | −30 |
| 桌機可見的學習重點列數 | 15 | **5** | −10 |
| 全頁中文字數（含收合內容） | 6,872 | 6,876 | 持平 |

手機逐區（390px）：

| 區塊 | v1 | v2 | 差 |
|---|---|---|---|
| TLDR | 473 | 486 | +13 |
| `#why` | 1,101 | 1,101 | 0 |
| `#syllabus` | 3,131 | 3,127 | −4 |
| `#class` | 1,343 | **956** | **−387** |
| `#course` | 2,178 | 2,222 | +44 |
| `#practice` | 3,819 | **3,505** | **−314** |
| `#report` | 1,105 | 1,105 | 0 |
| `#fit` | 728 | 728 | 0 |
| `#faq` | 1,775 | 1,925 | +150 |

**手機的加減互抵關係要記住**：圖示條列與 FAQ 分組是淨增高（TLDR +13、`#course` +44、`#faq` +150，合計 +207），
全部靠三條新的橫向滑動列賺回來（−701）。
⇒ **之後若要拿掉任何一條滑動列，手機就會變回比 v1 長**，改之前先量。

桌機少掉的 1,167px 全部來自三階段分頁。
字數持平是刻意的：v2 的目標是**重新排列**而不是刪內容，唯一真的刪掉的是 `.defbox` 第二段（雙師模式怎麼上），
因為同一件事在 `#tldr`、`#class`、FAQ 已經講過三次。

---

## 7. 改動 v2 之前的自檢清單

- [ ] `#tldr` 的文字有沒有被動到？（拿 `index.html` 逐字比對）
- [ ] FAQ 的 14 題題目與答案有沒有被動到？動了就要同步改 `FAQPage` schema
- [ ] 有沒有新增色票？有沒有出現官網亮橘 `#FFA300`？（本頁一律用平台 `#F5A623`）
- [ ] 有沒有彩色 emoji？
- [ ] 新加的圖示有沒有 `aria-hidden="true"`？旁邊有沒有文字？
- [ ] `data-oa-cta` 有沒有被加到 modal 按鈕以外的東西上？
- [ ] 分頁／摺疊隱藏的內容還在 DOM 裡嗎？（不可改成點開才載入）
- [ ] 桌機、手機、無 JS 三種情境都看過了嗎？
- [ ] 手機頁高有沒有變得比 `index.html` 長？（v1 基準 17,880px @390px）
- [ ] 動到任何 `.swipe` 列的話，`.step-num` 徽章還是完整的嗎？
