# DESIGN.md — 財商學習平台 landing page

> 視覺基準：Coin Quest 平台 staging 實測 token（2026-08-11 抽取，來源 `~/Documents/oa-coin-quest/research/staging-inventory.md` §四）。
> 定位：讓家長「看完官網進平台不會有落差」——官網長得跟產品一樣。
> 本頁**不使用**官網亮橘 #FFA300 色系（保旭拍板），但沿用官網的 CTA 意圖邏輯與 WCAG 紀律。

## 色彩 Token

| Token | 值 | 用途 | 對比（實算） |
|---|---|---|---|
| `--primary` | `#F5A623` | 主 CTA 底色、重點強調 | 配 `#2D2D2D` 字 = 6.79 ✅ AA；**配白字 ≈2.1 ❌ 禁** |
| `--gold` | `#FFBC57` | 招牌卡片邊框（4px 實線）、分隔裝飾 | 裝飾用，不做文字 |
| `--cream` | `#FFF9F0` | 頁面基底背景 | — |
| `--cream-2` | `#F9EDCF` | 區塊底色變化（平台導覽列色） | — |
| `--task` | `#FFFEEF` | 任務卡／引用卡背景 | — |
| `--card` | `#FFFFFF` | 一般卡片背景 | — |
| `--text` | `#2D2D2D` | 主文字 | 白底 13.6 ✅ |
| `--text-2` | `#666666` | 次要文字 | cream 底 5.4 ✅ AA |
| `--coral` | `#FF7B54` | 強調點綴——**僅限裝飾、邊框、大色塊**（色塊上配 `#2D2D2D`） | 配 `#2D2D2D` 5.38 ✅；配白 2.56 ❌ 禁白字；**當淺底文字 2.45 ❌ 禁** |
| `--coral-ink` | `#B84318` | 淺底上的強調小字（kicker、標籤） | cream 底 5.22 ✅ AA；白底 5.45 ✅ |
| `--teal` | `#4ECDC4` | 次要強調（平台簽到鈕色） | 配 `#2D2D2D` 7.11 ✅；禁白字 |
| `--dark` | `#242424` | 投資模組深色區、底部 CTA 帶 | 配白字 15.5 ✅ |
| `--invest-orange` | `#FFBD4A` | 深色區內的強調字 | 於 `#242424` 上 9.33 ✅ |
| `--gain` / `--loss` | `#4CAF50` / `#F44336` | 漲跌示意（僅深色區內；`--loss` 淺底僅限 ≥24px 粗體大字） | gain 於 dark 5.58 ✅；loss 白底 3.68＝僅過 AA 大字 |
| `--purple` | `#8E7CC3` | 週報「支出」長條示意（近似平台週報紫，僅裝飾、不做文字） | 裝飾用 |
| 深綠文字 | `#2E7D32` | 淺底上的「＋收入／已完成」文字（gain 本尊淺底不過 AA） | 白底 5.13 ✅ |

**禁用**：`#999999` 做任何文字（白底僅 2.85，AA 大字都不過；平台內部有用，landing 不跟）。

### Hero 專用（取自平台登入頁 `/welcome`，2026-08-13 實測）

| 用途 | 值 |
|---|---|
| 方格紙底紋 | `linear-gradient(rgba(245,166,35,.08) 1px, transparent 1px)` 雙向，`background-size: 26px` |
| 四色波浪（由上而下） | `#FF7B54` 珊瑚 → `#FFAD60` 橘 → `#FCD667` 黃 → `#4ECDC4` 青 |

波浪為**純裝飾**：內嵌 SVG（不增加請求）、`aria-hidden`、其上不放任何文字，故無對比要求。**四色只用於這條波浪**，不得挪作按鈕或文字色（`#FFAD60`／`#FCD667` 在淺底上做文字皆不過 AA）。

### 圓形舞台漸層（`.stage`）

| class | 徑向漸層 | 用途 |
|---|---|---|
| `t-teal` | `#E6F7F5 → #B4E5DE` | 波比、廚師服 |
| `t-gold` | `#FFF7E6 → #FFE0A6` | 露比、學校制服 |
| `t-coral` | `#FFF1EA → #FFD0BB` | 叩叮 |
| `t-sky` | `#EEF3FF → #C4D5F3` | 太空裝 |

## 字體

- `font-family: "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif`
- 根字級 **18px（手機 17px）**，對齊 classroom／about；`body` 用 `1rem` 繼承，勿寫死 px
- 行高 1.75；H1 clamp(34px→52px)；區塊標 clamp(26px→36px)
- **小字下限 12px**，且必須在**手機 17px 根字級下**仍成立 → rem 值不得低於 `0.71rem`
- **唯一例外：mockup 內部的介面模擬文字**（`.mock-*`、`.wr-*`、`.scene-bar`、`.game-hud` 等，皆 `aria-hidden`）。它們模擬的是 App UI、本來就比內文小，且在 375px 若不縮會斷行破版。此例外僅限裝飾性 mockup，真實頁面文字一律守 12px。
- 字重：內文 400、強調與數字 700、標題 800

## 圓角・陰影・邊框

- 招牌圓角 **15px**（卡片、按鈕、mockup）；徽章與 pill 用全圓角
- 招牌「硬邊卡通陰影」：`0 4px 4px rgba(0,0,0,.25)`（無模糊感的位移陰影）；hover：`0 4px 16px rgba(0,0,0,.2)` ＋ translateY(-2px)
- 資料型列表用柔和陰影 `0 1px 3px rgba(0,0,0,.1)`
- 招牌卡片邊框：`4px solid #FFBC57`；次要卡 `2px`

## CTA 意圖系統（沿用官網紀律）

| 意圖 | 樣式 | data-oa-cta |
|---|---|---|
| 留單（唯一主行動） | `#F5A623` 實心＋`#2D2D2D` 字＋`border:2px solid transparent`＋硬邊陰影 | `hero` / `middle` / `bottom` / `sticky` |
| 區塊內導覽 | 文字連結＋`→` | 不加 |

橘實心**只給留單**，不給任何其他行動；同頁所有留單鈕樣式一致。

## Iconography

彩色 emoji 全禁（含引用平台 UI 文案時，一律去 emoji）。僅允許功能性單色字符：★ ▸ → ← › ✓ ⋯。插圖用叩叮／波比真素材（`~/Desktop/各種叩叮/`）或占位區，**禁 AI 生成吉祥物與 logo**。

## 角色與造型的呈現：圓形舞台

角色素材多為淺色去背（波比是白鳥、造型多為白色衣服），**直接放白卡上會糊掉**。一律用 `.stage` 圓形舞台承載：

- 圓形、`3px solid var(--gold)` 邊、內陰影 `inset 0 -10px 20px rgba(0,0,0,.05)` 製造淺淺的舞台凹感
- 底色用**徑向漸層**（中心亮、邊緣深），四種色調對應不同角色／情境：`t-teal`（波比、廚師服）、`t-gold`（露比、制服）、`t-coral`（叩叮）、`t-sky`（太空裝）
- 角色圖寬 88%、加 `filter: drop-shadow(0 5px 7px rgba(0,0,0,.18))` 讓它從舞台浮起來
- 舞台是裝飾層、其上不放文字，故無 WCAG 對比要求；但邊框色須維持 `--gold` 與全站一致

## 圖片格式

除 `og-financial.jpg` 外，**所有圖片一律 WebP**（含 3 幀動畫角色，animated WebP 保留動畫）。2026-08-13 全面轉換：716KB → 170KB。og:image 維持 JPEG——社群爬蟲對 WebP 的支援不穩，分享卡會開天窗。

## 產品元素 mockup 規範

頁面的說服力來自「長得就像產品」。每個機制描述旁邊都放對應的產品 UI CSS mockup，**數據一律取自 staging 實測**（README 對照表可回溯）：hero 儀表板（任務／簽到／資產配置）、等級加薪條（Lv.1→Lv.2 真實日薪）、簽到月曆格、命運卡×保險理賠對照卡、課程地圖關卡路徑（真實關卡標題）、保險理賠 chips（真實理賠額）、商城分類頁籤＋等級門檻、排行榜頒獎台、週報收支長條（橘收入／紫支出，同平台週報配色邏輯）。mockup 不得杜撰數字；金額一律是平台模擬金幣、不加「元」。

## 完整課表區（`#syllabus`，2026-08-13 新增）

課程說服力的主結構，資料來源＝`research/course-outline.md`（教學部課程概要 PDF）。三個元件：

| 元件 | 樣式 | 對比（375px／17px 根字級實測） |
|---|---|---|
| `.q-card` 疑問句卡 | `--task` 底、`2px` 金邊、圓角 15px、硬邊陰影；問句 `.q-ask` 800 字重，堂次標註 `.q-where` 用 `--coral-ink` | 問句 13.56 ✅／堂次 5.36 ✅ |
| `.q-lead` 區內導言 | 800 字重、`--text`，用於把讀者帶進下方卡片群（非標題，不進 `.sec-head`） | 13.56 ✅ |
| `.sub-head` 區內小標 | 一個 section 內部再分節時用；`h3` 為 `clamp(21px, 2.5vw, 28px)`／800，配一行 `--text-2` 說明。**不是卡片變體**，不受「卡片區不准出現非卡片變體」約束 | 11.83 ✅ |
| `.phase` 階段卡 | 招牌規格：`--card` 底、`4px` 金邊；`.phase-badge` 用 `--cream-2` 膠囊、`.phase-range` 用 `--coral-ink`；`.phase-el` 標籤為 `--cream` 底 `2px` 金邊膠囊 | badge 11.83 ✅／range 5.45 ✅／el 13.16 ✅ |
| `.syl` 15 堂課表（`.syl-head` 起始） | 直接放在階段卡內、**不再有第二層開關**；`.syl li` 奇數列 `--task` 斑馬紋；堂次 `<b>` 用 `--coral-ink`、課名 `<i>` 800 字重、說明 `<em>` 用 `--text-2` | 堂次 5.45 ✅／說明 5.65 ✅ |
| `.cost` 代價論證框（01 區） | `--card` 底、左側 `6px solid var(--coral)` 實線（與 `.defbox` 的 `--primary` 左框刻意分色：一個是定義、一個是代價） | 13.77 ✅ |

### 階段卡：摘要也收進去（2026-09-03）

原本卡片外面留著 `.phase-sum`（89–119px／張），三張卡合計 1058px。把摘要移進 `details` 之後 → **707px（省 351px）**，而**三個標題仍在外面**，那條「看懂日常生活裡的錢 → 看懂銀行、信用與投資 → 看懂國家與世界怎麼運轉」的弧線完整保留。

🔴 **評估過「改成分頁（tabs）」並否決**。分頁能省更多（約 680px），但兩個問題不能接受：①**語意錯配**——分頁的意思是「三選一」，但三階段是循序修習（第二階段需修畢第一階段），會讓家長誤以為可以挑一個報名 ②**弧線會斷**——一次只看得到一個標題，壓成四字標籤力道差太多。**循序內容不用分頁，用收合。**

### 階段卡的漸進揭露（2026-08-13 建立，2026-08-14 由兩層改為一層）

手機頁長實測：階段卡完整攤開時三張共 2,662px。收合後降到 1,047px。全頁演進：26.6（全攤開）→ 24.6（兩層折疊）→ **23.7 螢幕**（改回一層、並擴寫 01 痛點後）。

| 視窗 | `details.phase-more` | 看得到什麼 |
|---|---|---|
| ≥861px（桌機） | 恆開，`summary` 設 `display: none` | 學習元素、5 條目標、**15 堂課表全部攤開**，無需任何點擊 |
| ≤860px（手機） | 收合，`summary` 顯示為「看這個階段的學習重點與完整課表」 | 徽章＋堂次＋標題＋摘要；**點一次**就出現學習元素、5 條目標與 15 堂課表全文 |

**收合後必須留在外面的，是三張卡的標題**——「看懂日常生活裡的錢 → 看懂銀行、信用與投資 → 看懂國家與世界怎麼運轉」這條同心圓弧線是本區最強的說服力，不得藏進收合區。

實作紀律：
- HTML 寫 `<details class="phase-more" open>`，**預設展開**；由區塊後方的內聯 `<script>` 在 `max-width: 860px` 時關閉。**無 JS 時退化成全部展開**（頁面較長但內容不遺失），不是全部藏起來——方向不可顛倒。
- 🔴 **只准一層，不准再包第二層**（2026-08-14 修正）。原本 15 堂課表另外收在 `details.syllabus` 內，形成兩層：第一層 summary 寫「看…15 堂課表」、點開卻沒有課表，還要再點一次。家長、行銷、UIUX 三個獨立視角都指出同一件事——**全頁可信度最高的證據（45 堂課表）被藏在沒人會點到的深度**。代價是手機頁長多約 1 個螢幕、桌機多約 2,600px，**這個代價要付**。
- 該 script 就地放在 `#syllabus` 的 `</section>` 之後，利用解析順序避免「先展開再收合」的閃動；勿移到檔尾。
- 開關點擊高度實測 61px，對比 13.77 ✅。

- **≥860px 時 `.phase-goals` 與 `.syl` 走 `columns: 2`**（多欄排版，閱讀順序為上下再換欄，符合連續堂次）；375px 自動單欄。`li` 一律 `break-inside: avoid`。
- 最小字級＝`.syl b` 的 `0.75rem`＝手機 12.75px，**貼近 12px 下限但成立**；此區不得再往下調。
- 課表全展開時 375px **無橫向溢出**（2026-08-13 實測）。
- 課表旁必附免責句 `.syl-note`（見 CLAUDE.md 4e）。

## 標題行框要容得下字墨（2026-08-18）

中文粗體標題的**字墨高度會超過 `font-size`**：34px／800 字重實測字墨 48px，`line-height: 1.25` 的行框只有 42.5px——**字墨溢出行框 5.5px**。行框一旦比字墨小，下一個元素就只靠 `margin` 撐開間距，換一套字型度量（不同裝置的 fallback 字型）就可能視覺相撞。

- H1 `line-height` **不低於 1.32**（實測五種字型下與副標的淨間距皆 ≥21px；1.25 時只剩 16px）
- 檢查法：用 `Range.selectNodeContents(el).getBoundingClientRect()` 量**字墨**框，不要只看 `offsetHeight`（那是行框）。逐一換 PingFang TC／Heiti TC／system-ui／serif 重量，取最小間距
- 代價：H1 高度 +2.4px、副標 margin 18→22px，全頁長度不變

## 疑問句卡改橫向滑動＋課表後 CTA（2026-08-18）

實測手機（390×844）文字／圖片分布，發現**第 3–9 螢幕連續 7 個螢幕、2,332 字、圖片數 0**——第一張真實產品畫面要到第 10 螢幕。兩項處置：

- **8 張 `.q-card` 加入 `.swipe`**（沿用 05 那套機制，`data-swipe-label` 供鍵盤可達性 JS 掛載）：980px → **147px**，8 題全留。滑動列的 `role/tabindex` JS 選擇器同時由 `#practice .swipe[...]` 放寬為全頁 `.swipe[...]`。
- **課表結束處補 `.syl-cta`**：原本 CTA 之間隔了 0.7 → 17.2 共 16.5 個螢幕（sticky 之外沒有出口）。45 堂課表是全頁說服力最高的證據，**看完當下必須有出口**。補上後為 0.7 → 6.4 → 16.5 → 20.5。

全頁 21.5 → **20.7 螢幕**。

## hero 用 CSS mockup、內文用實拍——這條界線是尺寸決定的（2026-08-18）

全站正在把 CSS mockup 換成真實擷取，**但 hero 是例外，而且理由可量化**：hero 的視覺欄在手機上**只有 350px 寬**（桌機 471px）。2880px 的平台截圖塞進去只剩 **12.2%**，平台原本 28px 的介面文字會變成 **3.4px**；CSS mockup 的文字則是 10.9–11.6px，讀得到。

| | hero | 內文區塊 |
|---|---|---|
| 任務 | 3 秒定位「這是什麼」 | 舉證「這是真的」 |
| 尺寸 | 350–471px | 460–960px＋圖說 |
| 作法 | **CSS mockup**（會重排、字級不縮、DOM 文字可被索引、零位元組） | **真實擷取**＋figcaption |

hero mockup 已依平台實際畫面補上最具辨識度的元件：**半圓環資產配置**（`conic-gradient`，角度＝百分比×1.8deg）、**排名膠囊**、**「進入課程」票券＋叩叮**。數值一律取自 staging 實測快照（51,021／38-38-24／日薪 1,600／排名 2），README 可回溯。

三個踩過的坑：
- `.mock-body` 在手機仍是兩欄時，資產卡只剩約 135px，半圓環 104px 幾乎佔滿、圖例被擠成一字一行 → 手機改單欄
- 環圈與圖例**並排**在桌機資產欄（190px）一樣會擠爆 → 一律改上下堆疊
- `.hero-popi` 只要進到卡片範圍就會壓到滿寬置中的圖說 → `bottom` 要讓角色整個落在卡片基座之下（桌機 −76px／手機 −92px）

代價：hero 1.71 → 2.11 螢幕。全頁 21.5 →（疑問卡滑動）20.7 →（hero 升級）**21.1 螢幕**。

## 實拍照片與螢幕擷取是兩種證據，不要互相取代（2026-09-03）

頁面原本 10 個章節有 5 個零視覺，而現有的 4 段影片與 5 張擷取**全部都是螢幕畫面**——證明「產品是真的」，但沒有一張證明「有人在用」。照片補的是後者，不是影片的次級品。

- **`.photo-pair`**：兩張並排（桌機 511×2，手機單欄）。用「序列」補回靜態照片沒有的流程感——兩張圖說串成一句話，勝過兩張各自為政
- 照片一律 16:9（原始 3:2 上下裁切），與頁面影片的 2.15–2.38 比例接近，同列不會撐高
- 放置原則：**照片放「家長需要想像自己孩子」的地方**（05 課後的練習場、07 適合誰），**不放痛點段**（01 的論證是「代價」，笑臉照會抵銷）
- 🔴 借用其他產品線的素材時，圖說只描述**模式**不描述**人的角色**。`photo-teacher` 取自線上程式課影片，只用老師那幾格（畫面無程式內容），且必須註明「實際授課老師依班級安排」

代價：手機 21.1 → **23 螢幕**。可接受，因為**增加的是圖不是字**——先前診斷的問題是「連續 6 個螢幕零圖的文字沙漠」，多出來的 1.9 螢幕正好是它的反面。早段沙漠維持 3 個螢幕，而字數最高峰的 03（第 7–9 螢幕）現在有視覺了。

## 前段減重三招（2026-09-03，競品比較後）

實測兩個競品後確認**不能照搬**：富邦理財學堂啟動頁只有 1 螢幕／60 字，但它是免費公益站；芒狗狗商品頁只有 3.8 螢幕／226 字，但賣的是 NT$1,999 實體盒組（照片即商品）且流量來自 Meta 廣告（說服在廣告端完成）。**本頁要承接自然搜尋，說服必須自己完成**，砍到 4 螢幕會直接殺掉 GEO。

診斷後對症下三招：

1. **`.defbox` 手機收合**（`details.def-more`，沿用階段卡那套 script）：234 字的財商素養定義擋在第 4.4 螢幕，**522px → 81px**。文字留在 DOM，DefinedTermSet schema 與 AEO 完全不受影響。
2. **01 痛點段加 `popi-sad`**：角色本身就是「為零用錢用光而沮喪」，與該段訊息完全吻合，不必新素材。
3. **8 張疑問卡改自測框架**：「這些問題多數大人也答不出來」→「**先自己試試看：這 8 題，你答得出幾題？**」偷富邦的互動鉤子，成本是改一句話。

🔴 **角色與文字並排在手機是陷阱**：`.talk-row` 原本手機也維持 grid `1fr auto`，結果文字欄被壓到 230px、對話從 346px 撐到 **522px**，行長過短反而更難讀。改成手機堆疊（角色靠右置於對話下方，110px），文字回到 372px，總高反而更低。**並排只在文字欄仍有 ≥600px 時才成立。**

成效：手機 23 → **22.7 螢幕**，前段連續零圖從 3 個螢幕降到 2 個。

## 內文一律滿版，不設行長上限（2026-09-03 保旭拍板）

🔴 **全站文字不得出現「中間就換行」。** 所有內文與標題一律撐滿容器（1080 容器 → 內容 1040，右緣 1060），與卡片、網格、圖片同寬。

**這是刻意推翻排版慣例的決定。** 慣例會建議中文行長 35–45 字以利閱讀（本頁滿版是 58 字）。但本頁的卡片、網格、照片列全是滿版，內文只要收窄，家長看到的就是「文字排到一半自己斷掉」——**整頁一致優先於單行可讀性**。這是保旭在實機（筆電 100% 縮放）上看過三次後拍板的。

實作：`:root` 的 `--measure: none`，`.lead`／`.q-lead`／`.type-line`／`.syl-note`／`.talk`／`.sub-head`／`.sec-head` 全部 `max-width: var(--measure)`。**要改回行長控制只需把這個變數改成 px 數**，其餘規則都吃它。

### 走過的兩次彎路（別再犯）

1. **曾把 `--measure` 套到有背景色的盒子上**：`.defbox` 從滿版 1028 縮到 778，而它正下方的疑問卡列仍是滿版 1040，白框看起來像壞掉。**盒子是結構元素，永遠滿版。**
2. **曾以為「統一行長」就能解決**：先設 720、再放寬到 860，兩次都被退回。問題從來不是「不一致」，是**只要比旁邊的滿版元素窄，就會被看成排版錯誤**。

### 剩下的合理空隙（不是 bug，不要修）

| 位置 | 右側空隙 | 原因 |
|---|---|---|
| hero 內文 | 539px | hero 是左右兩欄，右欄是平台 mockup |
| 痛點對話 `.talk` | 224px | `.talk-row` 是 grid，右邊是角色波比（180＋gap 24＋內距 22） |
| `.defbox`／`.cost` 內文 | 46px | 盒子本身的左右內距 26px |

## 兩個排版鐵律（2026-08-13 因實際破版補記）

1. **不准手切欄位**。多欄區塊一律用 CSS grid／multi-column 讓瀏覽器自動分配，**禁止用 `.xxx-col` 之類的容器把項目手動分成左右兩堆**——只要有人新增一個項目就會左右不均（FAQ 曾因此變成左 4 右 6）。FAQ 現行做法：13 個 `<details>` 直接放進 `.faq-list`（`grid-template-columns: 1fr 1fr; align-items: start`），奇數列左、偶數列右，永遠不會歪超過一項。
2. **卡片區塊裡不准出現「非卡片」的例外變體**。曾有 `.card-lite`（透明底、無邊框、只留左側線）只用在排行榜一處，在一整排 4px 金邊白卡中看起來像沒做完，且 DESIGN.md 未收錄。已改為 `.mod-rank`：比照 `.mod-invest`／`.mod-shop` 的滿版卡（`grid-column: 1 / -1`，左文右 mockup）。**要新增卡片變體，先寫進本檔再用。**

`.rank-stage` 頒獎台 mockup：三塊 `<i>` 內含名次數字 1/2/3，第一名用 `--primary` 底（深墨字 6.80 ✅），二三名用 `--cream-2`（11.83 ✅）；容器 `aria-hidden`，純裝飾。

## 橫向滑動列 `.swipe`（2026-08-13，僅手機生效）

05 課後的練習場原為 5,922px（7.3 螢幕），是全頁最長的一段。改用橫向滑動後降到 3,703px（4.6 螢幕）。**桌機完全不套用**，版面與改版前逐格相同。

| 位置 | 手機 | 桌機 |
|---|---|---|
| `.flow.swipe`（步驟 01–03） | 三張等高卡橫向捲動，卡寬 84%、露出下一張約 60px | `.flow` 原本的 3 欄 grid |
| `.flow-2`（步驟 04–06） | **維持直排**，自然高度 163/192/192 | 第二個 3 欄 grid，`margin-top: 20px` ＝ 原 grid gap |
| `.mod-swipe.swipe`（銀行／保險／商城／排行榜） | 四張橫向捲動，卡寬 92%（含 mockup，太窄會狂換行反而變高），露出約 33px | `display: contents`，四張直接參與 `.modules` 兩欄 grid，`mod-shop`／`mod-rank` 維持滿版 |
| `.mod-invest` | **不進滑動列**，維持滿版直排 | 滿版 |

**鐵律：高度差過大的卡片不放同一條滑動列。**

**2026-09-03 新增：03「一堂課怎麼上」的三張卡也進滑動列。** 實測三卡 247／190／247px，**高度差只有 1.3 倍**（本鐵律的教訓案例是 477 vs 202 ＝ 2.4 倍），短卡在等高列裡只空出 57px，遠低於當初的 275px。手機 725px → **294px，省 431px**。判準是倍數不是絕對值：**1.3 倍可以進同一列，2.4 倍不行。**

⚠️ 實作細節：手機單欄規則要寫成 `.grid-3:not(.swipe)`，否則 `grid-template-columns: 1fr` 會跟 `.swipe` 的 `display:flex` 打架。
 滑動列高度由最高的卡決定，短卡會空掉一大片。實測步驟卡 477px vs 202px（差 2.4 倍），六張放同列時後三張各空約 275px，看起來像沒做完——所以才拆成 `.flow.swipe` ＋ `.flow-2`。

其他紀律：
- 卡片內容一律**貼齊頂端**。`.step-num` 徽章釘在卡片上緣，若改成垂直置中，徽章與標題之間會斷開一大段。
- 單張卡不可避免的留白，用**版面錨點**吸收而非置中：`.mod-swipe .mod-rank { align-content: space-between }` 把頒獎台推到卡片底部（該卡留白 218px，是四張裡唯一明顯的）。
- 負邊距 `margin: 0 -20px` ＋ `padding: 0 20px` 讓卡片貼齊視窗兩側，**露出下一張的邊緣是主要的可滑提示**；另加 `.swipe-hint` 文字（`--coral-ink`，桌機 `display: none`）。
- 滑動容器需 `tabindex="0"`＋`role="group"`＋`aria-label`（WCAG 2.1.1 可捲動區域須鍵盤可達），並給 `:focus-visible` 外框。
- `scroll-snap-type: x mandatory` 搭配 `scroll-padding-left: 20px`，讓吸附位置對齊 `.wrap` 的內距。
- **`.mod-invest` 永遠不進滑動列**：投資模組的安全聲明必須跟著它一起被看見（見 CLAUDE.md 與 README 對照表）。

## 平台實拍 `.shot`（2026-08-17 起，取代 CSS mockup 的優先選項）

**有真實截圖就用真實截圖，CSS mockup 只是替身**——說服力來自「這就是產品」。

| 變體 | 用途 | 規格 |
|---|---|---|
| `.shot` | 滿版區塊（課程地圖、投資模組） | `--card` 底、4px 金邊、圓角 15px、`padding: 10px 10px 0`，內圖 `border-radius: 8px` |
| `.shot-tight` | 卡片內嵌（卡片自己已有金邊，內框降為 2px、6px padding） | 疊加在 `.shot` 上 |
| `.shot-in-dark` | 深色卡內（投資卡） | 底 `#1B1B1B`、框 `#3A3A3A`、figcaption `#C9C4BC` |

紀律：
- **一律附 `figcaption` 標註「平台實際畫面」**——真實性是它存在的理由，要讓讀者知道
- 截圖**只裁單一 UI 元件**，不放整個瀏覽器視窗；窄容器（≤510px）內的截圖，原圖文字在顯示尺寸下必須可讀
- 檔名一律 `shot-*.webp`；來源原始檔存 `~/Downloads/oa-coin-quest-video/capture/stills_raw/`
- 換上 `.shot` 後，被取代的 CSS mockup 樣式**要一併刪除**（勿留 dead CSS）
- 圖內若含數字（利率、理賠額、筆數），視為對外數字，**須列入 README 對照表**

## 平台實錄影片（2026-08-18 新增）

第一批真實平台錄影（Chrome 驅動已登入的 staging、`screencapture` 錄製、ffmpeg 裁切），**取代對應位置的 CSS mockup**。錄製與後製管線見 `~/Documents/oa-coin-quest/video/07_官網用片段規格.md`。

| 檔名 | 位置 | 取代了什麼 |
|---|---|---|
| `vid-unit_done` | 04（課程地圖後） | 新增——課程影片實際播放 12 秒 |
| `vid-invest` | 05 投資滿版卡 | `shot-invest.webp`（零持股的靜態圖） |
| `vid-savings` | 05 銀行卡 | 靜態截圖（改為完整存款操作） |
| `vid-report` | 06 週報 | CSS 週報 mockup |

**滿版卡影片的手機取捨（2026-08-18，invest）**：`vid-invest` 桌機 956px 文字全可讀（三檔持股切換＋清單底部「共 1366 筆」），手機只有 251px、文字不可讀。仍然採用，理由是**它取代的 `shot-invest.webp` 在同一容器內也是 251px、同樣不可讀**——不是退步，而是桌機明顯變強、手機持平；圖說本來就承擔實際論證。**這條不可推廣成「窄卡也能放影片」**：`checkin` 被否決正是因為它取代的 CSS 月曆在 333px 內反而讀得到。判準是「跟它取代的東西比」，不是絕對尺寸。

🔴 **游標會被錄進去——關掉它，不要事後修（2026-08-18）**：平台自訂了 CSS 游標（白箭頭橘描邊），`screencapture` 連錄影帶截圖都會帶進畫面。`document.elementFromPoint` 找不到它，因為那是瀏覽器繪製的游標、不是 DOM 元素。**解法：擷取前先注入 `*, *::before, *::after { cursor: none !important; }`**（id `__nocur`），一行解決，不必事後修圖。

🔴 **擷取區座標要實測校準，不要用 outerHeight−innerHeight 推算（2026-08-18）**：推算出的內容區頂端跟實際差了 34pt，導致每張截圖都少了最上面 34pt、區塊標題被切一半。**校準法：把已知元素捲到視窗某位置，截一張，看它落在截圖的哪裡，回推偏移。** 本機實測值＝`screencapture -R 0,176,1440,673`（視窗 bounds `{0,25,1440,875}`）。

**長頁面用「分段定格」而不是「整頁捲動」（2026-08-18，report）**：週報頁 14,482px，其中 **86% 是「本週完成的學習單元」179 個單元的清單**——整頁捲動 80% 的時間都在刷無意義的列表，而且捲太快什麼都讀不到。改成三段定格（各 3 秒、0.6 秒淡接、末段淡回首段成無縫迴圈），三段刻意對齊該章節文案的三個賣點：①本週摘要＋可以和孩子一起聊聊 ②上課進度（活躍度／等級／完成單元）③理財狀況（資產配置／收支）。**由靜態截圖合成，不錄影**：沒有游標、框取精準、幾乎不佔體積——324KB（捲動版）→ **79KB**。判準：**內容是「幾個重點區塊」就分段定格，是「連續變化的過程」才錄影。**

🔴 **錄影前隱藏測試橫幅不夠，要驗成品（2026-08-18，report）**：錄影是背景啟動、隱藏橫幅的 JS 是之後才送達，中間有數秒延遲，**黃色「此為測試環境」橫幅會留在片頭**。已加自動檢測（掃前 20 列的高黃像素比例）。同一次還發現舊的 report 片段**根本沒捲到**——81KB 的檔案幾乎全靜止，是因為取窗落在動作開始前的靜止段。**每段成品都要跑：①黃橫幅檢測 ②垂直位移 ③首尾差異（<6 才算可無縫迴圈）**。

**媒體比例要跟同列其他卡對齊（2026-08-18，保險卡）**：`.mod-swipe` 滑動列高度由最高的卡決定。保險卡原用單張醫療險特寫（比例 1.12、手機 210px 高），同列影片是 2.15–2.2（108–117px 高），保險卡因此撐高整列，而且**文案寫「9 種保險商品」、圖只有 1 種**。改裁成 3×2 六格（比例 1.63、145px），六種險名與理賠金額在桌機 460px 全可讀，手機也一眼看出「有很多種」。**判準：媒體比例與同列其他卡的差距，跟文案宣稱的數量，兩者要一起看。**

🔴 **裁切起點必須避開版面跳動（2026-08-18）**：`unit_done` 初版從 raw 5.0s 起裁，開頭 1 秒內畫面整體上移 17px（≈原始 113px）——那是播放器就位時的一次性瞬跳，不是內容。逐幀量垂直位移後改由 6.05s 起裁。**新錄任何一段，都要先量開頭有沒有這種位移**：以末幀為基準做逐列灰階剖面互相關，找出「自己與其後全部皆為 0」的最早時間點當起點。捲動類另需確認尾端回到起點，迴圈才無縫。

🔴 **兩處嵌了又回退（2026-08-18，錄了不等於要用）**：①商城卡維持三張造型 figure——「900 金幣・Lv.2 → 1,800 金幣・Lv.4」的價格階梯是卡片文案的論證，影片頁籤掃過即逝反而弱 ②簽到流程卡維持 CSS 月曆——333px 卡內影片文字不可讀。兩段成品在 `capture/web/` 供銷售影片剪輯用。**每段影片必須贏過它取代的東西，否則不上。**

鐵律：
- 一律 `<video class="pvid" preload="none" muted playsinline loop>` ＋ `poster`（首幀 WebP）＋ `width/height`（防 CLS）＋ `aria-label` 描述內容
- **播放由 JS 控制**：IntersectionObserver 進視窗播、離開暫停；`prefers-reduced-motion: reduce` 一律不播（只見 poster）；無 JS 時就是一張 poster 圖——**不寫 `autoplay` 屬性**，方向不可顛倒
- 單檔 ≤400KB、無音軌、H.264 yuv420p `+faststart`；目前五段合計 ~890KB
- ⚠️ **preview pane 驗不了自動播放**：Chrome 對背景分頁的省電策略會擋 video-only media（`play()` 直接 reject）。要驗請用前景分頁
- 投資模組影片待 16:00 開盤錄製；保險卡**刻意保留靜態特寫**（醫療險 $3,000 與命運卡敘事連動，比列表捲動更有說服力）

## 🔴 滑動列會裁掉絕對定位的徽章（2026-09-03，v2 廢棄前撈出）

`.swipe` 的 `overflow-x: auto` **會讓 `overflow-y` 一併變成 `auto`**（CSS 規範：兩軸不能一個 visible 一個 auto）。
於是任何 `top` 為負值、突出卡片上緣的元素，都會被容器在 padding box 邊緣裁掉。

實際案例：`.step-num`（01–06 那顆橘色徽章）是 `top: -18px`，基礎 `.swipe` 只有 `padding: 4px 20px 14px`，
**實測徽章上緣被裁 10px**（375px 寬）。步驟 04–06 那條列不是滑動列（`class="flow flow-2"`，無 `.swipe`），所以沒事——
這也是為什麼肉眼容易漏掉：同一區兩條列只有一條壞。

```css
@media (max-width: 860px) {
  .flow.swipe { padding-top: 22px; }   /* 容得下 top:-18px 的徽章 + 4px 餘裕 */
}
```

**通則**：往 `.swipe` 放任何帶負值定位的裝飾（徽章、緞帶、角標）之前，先確認上／下內距容得下它。
驗證方法是量，不是看——`clipped = max(0, 容器.getBoundingClientRect().top − 徽章.getBoundingClientRect().top)`，
修正前後各量一次，才知道自己真的修好了（本次實測 10px → 0px）。

## 版式原則

- 淺色奶油底＋高飽和暖色點綴＋粗金邊框＋硬邊陰影＝「兒童遊戲化但不廉價」
- **深色例外（僅此三處）**：①投資模組卡 ②底部 CTA 帶——皆用 `#242424`，複刻平台「投資介面刻意做成正式看盤軟體」的訊號，也讓橘實心鈕在深色帶上跳出來 ③課堂實境的小遊戲卡 `.game-scene`——深藍夜空漸層 `#1B2A5E → #2E4C8F → #23407A`（複刻遊戲實際畫面「深藍夜空＋草原剪影」，course-content §五），白字對比 13.0/7.9 過 AA
- 不自帶 header/footer；body 從 skip-link＋breadcrumb 開始
- 行動版 sticky 留單鈕（捲動 600px 後出現，`hidden` 起始、JS 移除）
