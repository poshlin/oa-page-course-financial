# DESIGN.md — 財商學習平台 landing page

> 視覺基準：Coin Quest 平台 staging 實測 token（2026-08-11 抽取，來源 `~/Documents/oa-coin-quest/research/staging-inventory.md` §四）。
> 定位：讓家長「看完官網進平台不會有落差」——官網長得跟產品一樣。
> 本頁**不使用**官網亮橘 #FFA300 色系（保旭拍板），但沿用官網的 CTA 意圖邏輯與 WCAG 紀律。

## 色彩 Token

| Token | 值 | 用途 | 對比（實算） |
|---|---|---|---|
| `--primary` | `#F5A623` | 主 CTA 底色、重點強調 | 配 `#2D2D2D` 字 = 6.79 ✅ AA；**配白字 ≈2.1 ❌ 禁** |
| `--gold` | `#FFBC57` | 招牌卡片邊框（4px 實線）、分隔裝飾 | 裝飾用，不做文字 |
| `--cream` | `#FFF9F0` | 頁面基底背景 | — |
| `--cream-2` | `#F9EDCF` | 區塊底色變化（平台導覽列色） | — |
| `--task` | `#FFFEEF` | 任務卡／引用卡背景 | — |
| `--card` | `#FFFFFF` | 一般卡片背景 | — |
| `--text` | `#2D2D2D` | 主文字 | 白底 13.6 ✅ |
| `--text-2` | `#666666` | 次要文字 | cream 底 5.4 ✅ AA |
| `--coral` | `#FF7B54` | 強調點綴——**僅限裝飾、邊框、大色塊**（色塊上配 `#2D2D2D`） | 配 `#2D2D2D` 5.38 ✅；配白 2.56 ❌ 禁白字；**當淺底文字 2.45 ❌ 禁** |
| `--coral-ink` | `#B84318` | 淺底上的強調小字（kicker、標籤） | cream 底 5.22 ✅ AA；白底 5.45 ✅ |
| `--teal` | `#4ECDC4` | 次要強調（平台簽到鈕色） | 配 `#2D2D2D` 7.11 ✅；禁白字 |
| `--dark` | `#242424` | 投資模組深色區、底部 CTA 帶 | 配白字 15.5 ✅ |
| `--invest-orange` | `#FFBD4A` | 深色區內的強調字 | 於 `#242424` 上 9.33 ✅ |
| `--gain` / `--loss` | `#4CAF50` / `#F44336` | 漲跌示意（僅深色區內；`--loss` 淺底僅限 ≥24px 粗體大字） | gain 於 dark 5.58 ✅；loss 白底 3.68＝僅過 AA 大字 |
| `--purple` | `#8E7CC3` | 週報「支出」長條示意（近似平台週報紫，僅裝飾、不做文字） | 裝飾用 |
| 深綠文字 | `#2E7D32` | 淺底上的「＋收入／已完成」文字（gain 本尊淺底不過 AA） | 白底 5.13 ✅ |

**禁用**：`#999999` 做任何文字（白底僅 2.85，AA 大字都不過；平台內部有用，landing 不跟）。

### Hero 專用（取自平台登入頁 `/welcome`，2026-08-13 實測）

| 用途 | 值 |
|---|---|
| 方格紙底紋 | `linear-gradient(rgba(245,166,35,.08) 1px, transparent 1px)` 雙向，`background-size: 26px` |
| 四色波浪（由上而下） | `#FF7B54` 珊瑚 → `#FFAD60` 橘 → `#FCD667` 黃 → `#4ECDC4` 青 |

波浪為**純裝飾**：內嵌 SVG（不增加請求）、`aria-hidden`、其上不放任何文字，故無對比要求。**四色只用於這條波浪**，不得挪作按鈕或文字色（`#FFAD60`／`#FCD667` 在淺底上做文字皆不過 AA）。

### 圓形舞台漸層（`.stage`）

| class | 徑向漸層 | 用途 |
|---|---|---|
| `t-teal` | `#E6F7F5 → #B4E5DE` | 波比、廚師服 |
| `t-gold` | `#FFF7E6 → #FFE0A6` | 露比、學校制服 |
| `t-coral` | `#FFF1EA → #FFD0BB` | 叩叮 |
| `t-sky` | `#EEF3FF → #C4D5F3` | 太空裝 |

## 字體

- `font-family: "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif`
- 根字級 **18px（手機 17px）**，對齊 classroom／about；`body` 用 `1rem` 繼承，勿寫死 px
- 行高 1.75；H1 clamp(34px→52px)；區塊標 clamp(26px→36px)
- **小字下限 12px**，且必須在**手機 17px 根字級下**仍成立 → rem 值不得低於 `0.71rem`
- **唯一例外：mockup 內部的介面模擬文字**（`.mock-*`、`.wr-*`、`.scene-bar`、`.game-hud` 等，皆 `aria-hidden`）。它們模擬的是 App UI、本來就比內文小，且在 375px 若不縮會斷行破版。此例外僅限裝飾性 mockup，真實頁面文字一律守 12px。
- 字重：內文 400、強調與數字 700、標題 800

## 圓角・陰影・邊框

- 招牌圓角 **15px**（卡片、按鈕、mockup）；徽章與 pill 用全圓角
- 招牌「硬邊卡通陰影」：`0 4px 4px rgba(0,0,0,.25)`（無模糊感的位移陰影）；hover：`0 4px 16px rgba(0,0,0,.2)` ＋ translateY(-2px)
- 資料型列表用柔和陰影 `0 1px 3px rgba(0,0,0,.1)`
- 招牌卡片邊框：`4px solid #FFBC57`；次要卡 `2px`

## CTA 意圖系統（沿用官網紀律）

| 意圖 | 樣式 | data-oa-cta |
|---|---|---|
| 留單（唯一主行動） | `#F5A623` 實心＋`#2D2D2D` 字＋`border:2px solid transparent`＋硬邊陰影 | `hero` / `middle` / `bottom` / `sticky` |
| 區塊內導覽 | 文字連結＋`→` | 不加 |

橘實心**只給留單**，不給任何其他行動；同頁所有留單鈕樣式一致。

## Iconography

彩色 emoji 全禁（含引用平台 UI 文案時，一律去 emoji）。僅允許功能性單色字符：★ ▸ → ← › ✓ ⋯。插圖用叩叮／波比真素材（`~/Desktop/各種叩叮/`）或占位區，**禁 AI 生成吉祥物與 logo**。

## 角色與造型的呈現：圓形舞台

角色素材多為淺色去背（波比是白鳥、造型多為白色衣服），**直接放白卡上會糊掉**。一律用 `.stage` 圓形舞台承載：

- 圓形、`3px solid var(--gold)` 邊、內陰影 `inset 0 -10px 20px rgba(0,0,0,.05)` 製造淺淺的舞台凹感
- 底色用**徑向漸層**（中心亮、邊緣深），四種色調對應不同角色／情境：`t-teal`（波比、廚師服）、`t-gold`（露比、制服）、`t-coral`（叩叮）、`t-sky`（太空裝）
- 角色圖寬 88%、加 `filter: drop-shadow(0 5px 7px rgba(0,0,0,.18))` 讓它從舞台浮起來
- 舞台是裝飾層、其上不放文字，故無 WCAG 對比要求；但邊框色須維持 `--gold` 與全站一致

## 圖片格式

除 `og-financial.jpg` 外，**所有圖片一律 WebP**（含 3 幀動畫角色，animated WebP 保留動畫）。2026-08-13 全面轉換：716KB → 170KB。og:image 維持 JPEG——社群爬蟲對 WebP 的支援不穩，分享卡會開天窗。

## 產品元素 mockup 規範

頁面的說服力來自「長得就像產品」。每個機制描述旁邊都放對應的產品 UI CSS mockup，**數據一律取自 staging 實測**（README 對照表可回溯）：hero 儀表板（任務／簽到／資產配置）、等級加薪條（Lv.1→Lv.2 真實日薪）、簽到月曆格、命運卡×保險理賠對照卡、課程地圖關卡路徑（真實關卡標題）、保險理賠 chips（真實理賠額）、商城分類頁籤＋等級門檻、排行榜頒獎台、週報收支長條（橘收入／紫支出，同平台週報配色邏輯）。mockup 不得杜撰數字；金額一律是平台模擬金幣、不加「元」。

## 完整課表區（`#syllabus`，2026-08-13 新增）

課程說服力的主結構，資料來源＝`research/course-outline.md`（教學部課程概要 PDF）。三個元件：

| 元件 | 樣式 | 對比（375px／17px 根字級實測） |
|---|---|---|
| `.q-card` 疑問句卡 | `--task` 底、`2px` 金邊、圓角 15px、硬邊陰影；問句 `.q-ask` 800 字重，堂次標註 `.q-where` 用 `--coral-ink` | 問句 13.56 ✅／堂次 5.36 ✅ |
| `.q-lead` 區內導言 | 800 字重、`--text`，用於把讀者帶進下方卡片群（非標題，不進 `.sec-head`） | 13.56 ✅ |
| `.sub-head` 區內小標 | 一個 section 內部再分節時用；`h3` 為 `clamp(21px, 2.5vw, 28px)`／800，配一行 `--text-2` 說明。**不是卡片變體**，不受「卡片區不准出現非卡片變體」約束 | 11.83 ✅ |
| `.phase` 階段卡 | 招牌規格：`--card` 底、`4px` 金邊；`.phase-badge` 用 `--cream-2` 膠囊、`.phase-range` 用 `--coral-ink`；`.phase-el` 標籤為 `--cream` 底 `2px` 金邊膠囊 | badge 11.83 ✅／range 5.45 ✅／el 13.16 ✅ |
| `.syl` 15 堂課表（`.syl-head` 起始） | 直接放在階段卡內、**不再有第二層開關**；`.syl li` 奇數列 `--task` 斑馬紋；堂次 `<b>` 用 `--coral-ink`、課名 `<i>` 800 字重、說明 `<em>` 用 `--text-2` | 堂次 5.45 ✅／說明 5.65 ✅ |
| `.cost` 代價論證框（01 區） | `--card` 底、左側 `6px solid var(--coral)` 實線（與 `.defbox` 的 `--primary` 左框刻意分色：一個是定義、一個是代價） | 13.77 ✅ |

### 階段卡的漸進揭露（2026-08-13 建立，2026-08-14 由兩層改為一層）

手機頁長實測：階段卡完整攤開時三張共 2,662px。收合後降到 1,047px。全頁演進：26.6（全攤開）→ 24.6（兩層折疊）→ **23.7 螢幕**（改回一層、並擴寫 01 痛點後）。

| 視窗 | `details.phase-more` | 看得到什麼 |
|---|---|---|
| ≥861px（桌機） | 恆開，`summary` 設 `display: none` | 學習元素、5 條目標、**15 堂課表全部攤開**，無需任何點擊 |
| ≤860px（手機） | 收合，`summary` 顯示為「看這個階段的學習重點與完整課表」 | 徽章＋堂次＋標題＋摘要；**點一次**就出現學習元素、5 條目標與 15 堂課表全文 |

**收合後必須留在外面的，是三張卡的標題**——「看懂日常生活裡的錢 → 看懂銀行、信用與投資 → 看懂國家與世界怎麼運轉」這條同心圓弧線是本區最強的說服力，不得藏進收合區。

實作紀律：
- HTML 寫 `<details class="phase-more" open>`，**預設展開**；由區塊後方的內聯 `<script>` 在 `max-width: 860px` 時關閉。**無 JS 時退化成全部展開**（頁面較長但內容不遺失），不是全部藏起來——方向不可顛倒。
- 🔴 **只准一層，不准再包第二層**（2026-08-14 修正）。原本 15 堂課表另外收在 `details.syllabus` 內，形成兩層：第一層 summary 寫「看…15 堂課表」、點開卻沒有課表，還要再點一次。家長、行銷、UIUX 三個獨立視角都指出同一件事——**全頁可信度最高的證據（45 堂課表）被藏在沒人會點到的深度**。代價是手機頁長多約 1 個螢幕、桌機多約 2,600px，**這個代價要付**。
- 該 script 就地放在 `#syllabus` 的 `</section>` 之後，利用解析順序避免「先展開再收合」的閃動；勿移到檔尾。
- 開關點擊高度實測 61px，對比 13.77 ✅。

- **≥860px 時 `.phase-goals` 與 `.syl` 走 `columns: 2`**（多欄排版，閱讀順序為上下再換欄，符合連續堂次）；375px 自動單欄。`li` 一律 `break-inside: avoid`。
- 最小字級＝`.syl b` 的 `0.75rem`＝手機 12.75px，**貼近 12px 下限但成立**；此區不得再往下調。
- 課表全展開時 375px **無橫向溢出**（2026-08-13 實測）。
- 課表旁必附免責句 `.syl-note`（見 CLAUDE.md 4e）。

## 兩個排版鐵律（2026-08-13 因實際破版補記）

1. **不准手切欄位**。多欄區塊一律用 CSS grid／multi-column 讓瀏覽器自動分配，**禁止用 `.xxx-col` 之類的容器把項目手動分成左右兩堆**——只要有人新增一個項目就會左右不均（FAQ 曾因此變成左 4 右 6）。FAQ 現行做法：13 個 `<details>` 直接放進 `.faq-list`（`grid-template-columns: 1fr 1fr; align-items: start`），奇數列左、偶數列右，永遠不會歪超過一項。
2. **卡片區塊裡不准出現「非卡片」的例外變體**。曾有 `.card-lite`（透明底、無邊框、只留左側線）只用在排行榜一處，在一整排 4px 金邊白卡中看起來像沒做完，且 DESIGN.md 未收錄。已改為 `.mod-rank`：比照 `.mod-invest`／`.mod-shop` 的滿版卡（`grid-column: 1 / -1`，左文右 mockup）。**要新增卡片變體，先寫進本檔再用。**

`.rank-stage` 頒獎台 mockup：三塊 `<i>` 內含名次數字 1/2/3，第一名用 `--primary` 底（深墨字 6.80 ✅），二三名用 `--cream-2`（11.83 ✅）；容器 `aria-hidden`，純裝飾。

## 橫向滑動列 `.swipe`（2026-08-13，僅手機生效）

05 課後的練習場原為 5,922px（7.3 螢幕），是全頁最長的一段。改用橫向滑動後降到 3,703px（4.6 螢幕）。**桌機完全不套用**，版面與改版前逐格相同。

| 位置 | 手機 | 桌機 |
|---|---|---|
| `.flow.swipe`（步驟 01–03） | 三張等高卡橫向捲動，卡寬 84%、露出下一張約 60px | `.flow` 原本的 3 欄 grid |
| `.flow-2`（步驟 04–06） | **維持直排**，自然高度 163/192/192 | 第二個 3 欄 grid，`margin-top: 20px` ＝ 原 grid gap |
| `.mod-swipe.swipe`（銀行／保險／商城／排行榜） | 四張橫向捲動，卡寬 92%（含 mockup，太窄會狂換行反而變高），露出約 33px | `display: contents`，四張直接參與 `.modules` 兩欄 grid，`mod-shop`／`mod-rank` 維持滿版 |
| `.mod-invest` | **不進滑動列**，維持滿版直排 | 滿版 |

**鐵律：高度差過大的卡片不放同一條滑動列。** 滑動列高度由最高的卡決定，短卡會空掉一大片。實測步驟卡 477px vs 202px（差 2.4 倍），六張放同列時後三張各空約 275px，看起來像沒做完——所以才拆成 `.flow.swipe` ＋ `.flow-2`。

其他紀律：
- 卡片內容一律**貼齊頂端**。`.step-num` 徽章釘在卡片上緣，若改成垂直置中，徽章與標題之間會斷開一大段。
- 單張卡不可避免的留白，用**版面錨點**吸收而非置中：`.mod-swipe .mod-rank { align-content: space-between }` 把頒獎台推到卡片底部（該卡留白 218px，是四張裡唯一明顯的）。
- 負邊距 `margin: 0 -20px` ＋ `padding: 0 20px` 讓卡片貼齊視窗兩側，**露出下一張的邊緣是主要的可滑提示**；另加 `.swipe-hint` 文字（`--coral-ink`，桌機 `display: none`）。
- 滑動容器需 `tabindex="0"`＋`role="group"`＋`aria-label`（WCAG 2.1.1 可捲動區域須鍵盤可達），並給 `:focus-visible` 外框。
- `scroll-snap-type: x mandatory` 搭配 `scroll-padding-left: 20px`，讓吸附位置對齊 `.wrap` 的內距。
- **`.mod-invest` 永遠不進滑動列**：投資模組的安全聲明必須跟著它一起被看見（見 CLAUDE.md 與 README 對照表）。

## 平台實拍 `.shot`（2026-08-17 起，取代 CSS mockup 的優先選項）

**有真實截圖就用真實截圖，CSS mockup 只是替身**——說服力來自「這就是產品」。

| 變體 | 用途 | 規格 |
|---|---|---|
| `.shot` | 滿版區塊（課程地圖、投資模組） | `--card` 底、4px 金邊、圓角 15px、`padding: 10px 10px 0`，內圖 `border-radius: 8px` |
| `.shot-tight` | 卡片內嵌（卡片自己已有金邊，內框降為 2px、6px padding） | 疊加在 `.shot` 上 |
| `.shot-in-dark` | 深色卡內（投資卡） | 底 `#1B1B1B`、框 `#3A3A3A`、figcaption `#C9C4BC` |

紀律：
- **一律附 `figcaption` 標註「平台實際畫面」**——真實性是它存在的理由，要讓讀者知道
- 截圖**只裁單一 UI 元件**，不放整個瀏覽器視窗；窄容器（≤510px）內的截圖，原圖文字在顯示尺寸下必須可讀
- 檔名一律 `shot-*.webp`；來源原始檔存 `~/Downloads/oa-coin-quest-video/capture/stills_raw/`
- 換上 `.shot` 後，被取代的 CSS mockup 樣式**要一併刪除**（勿留 dead CSS）
- 圖內若含數字（利率、理賠額、筆數），視為對外數字，**須列入 README 對照表**

## 版式原則

- 淺色奶油底＋高飽和暖色點綴＋粗金邊框＋硬邊陰影＝「兒童遊戲化但不廉價」
- **深色例外（僅此三處）**：①投資模組卡 ②底部 CTA 帶——皆用 `#242424`，複刻平台「投資介面刻意做成正式看盤軟體」的訊號，也讓橘實心鈕在深色帶上跳出來 ③課堂實境的小遊戲卡 `.game-scene`——深藍夜空漸層 `#1B2A5E → #2E4C8F → #23407A`（複刻遊戲實際畫面「深藍夜空＋草原剪影」，course-content §五），白字對比 13.0/7.9 過 AA
- 不自帶 header/footer；body 從 skip-link＋breadcrumb 開始
- 行動版 sticky 留單鈕（捲動 600px 後出現，`hidden` 起始、JS 移除）
