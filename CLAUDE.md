# oa-page-fin-platform（財商學習平台）

本專案的設計系統定義在根目錄的 `DESIGN.md`。產生或修改任何 UI 元件前，一律先讀它。

本 repo 的特殊規則（與其他 oa-page 不同，勿「好心修正」）：

1. **本頁採 Coin Quest 平台自身視覺**（保旭 2026-08-11 拍板）：主色是平台的 `#F5A623`，**不是**官網亮橘 `#FFA300`。看到 `#F5A623` 不是筆誤，禁止改回亮橘。實心鈕一律配深墨 `#2D2D2D` 字、禁白字，並寫 `border: 2px solid transparent`。
2. **留單 CTA**：`<button type="button" data-oa-cta="hero|middle|bottom|sticky">`，modal 由母站 shell 綁定提供；本 repo 本機開啟點了沒反應＝正常，不是 bug，別自己加 href 或 JS。
3. **不自帶 header/footer/nav**：注入官網時站頭站尾由官網提供。body 從 skip-link 直接進 hero（麵包屑亦不自帶，見 7c）。
4. **內容事實唯一來源**＝`~/Documents/oa-coin-quest/research/` 的 `deck-transcript.md` 與 `staging-inventory.md`。頁上每個數字都必須能回溯到這兩檔或品牌記憶（14 年／10 萬+ 學員）。
5. **價格與 SKU 組合（含平台使用權月數）一律不放**——定價 Albert 修正中。可放的課程數字：45 堂、每週一堂、90 分鐘（0-50 雙師／41-50 進度確認／51-90 財商議題課）、試聽 50 分鐘。
6. **產品定位（2026-08-12 保旭拍板，取代舊「純自學」定義）**：雙師課程＋平台，**禁再寫「純自學／無真人老師」**。雙師寫法紅線：真人老師「全程看得到畫面、發現卡住主動開啟通話、舉手發問依序輔導」——**禁寫「馬上一對一」「即時一對一」**（既有紅線，見記憶 feedback_oa_course_descriptions）。41-50 分鐘寫「確認進度」**禁寫「驗收」**（保旭原話：不額外驗收觀念）。會議工具寫「線上會議室」**禁寫 Google Meet**（工具未定案）。試聽=50 分鐘雙師＋平台，**不含財商議題課**，文案不得暗示含。
7. **「財商議題課」＝對外正式名稱；「動腦時間」＝孩子課表用語**——雙層命名是刻意設計，勿統一成單一名稱。
7b. **課程名「橘蘋財商素養課程」vs 平台名「財商學習平台」是兩個層級**：賣的是課程（H1／title／Course.name），孩子用的是平台（mockup 視窗標題／alternateName）。勿混用、勿統一。
7c. **不自帶麵包屑**（母站提供導覽）；但 JSON-LD `BreadcrumbList` 刻意保留，非可見元件，勿判為 schema 與可見內容不符。
8. 彩色 emoji 全站禁用（引用平台 UI 文案時把 emoji 去掉）；叩叮／波比素材禁 AI 生成，用真素材或占位區。
